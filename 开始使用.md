# 先从这里开始

这是一个可运行、可演示的 Codex Skill。所有项目代码、模板、测试、原始数据副本和输出都在本文件夹中。`node_modules` 是指向本机已安装依赖的目录联接，未复制或修改依赖。

## 5 分钟客户演示

1. 打开 `客户演示报告.pdf`，说明输入是四个固定格式文件，全部为模拟数据。
2. 双击 `一键演示.cmd`。它重新核对模拟数据，在新的 `运行结果/` 子目录导出并打开 Excel；发现异常属于正常结果。
3. 在报表的“异常清单”里，定位成本表第 9 行、订单表第 8/12 行，以及无法关联的退款。
4. 查看 `演示/确认后重跑/经营利润报表.xlsx`，说明商家按凭证确认后，十笔订单经营贡献为 610.50 元。另有一笔处理中退款 30 元尚未扣减。
5. 展示 `原始数据/` 和 `审计记录.json`，证明来源、行号、排除原因和计算金额可以逐项追溯。

本版不适配任意平台导出。订单格式只有一种：固定四表 v1。空白模板在 `空白模板/`，字段口径见 `ecommerce-reconcile/references/输入格式与口径.md`。

## 给真实商家使用

商家将四个模板文件填好并通过当前 Codex 对话上传，或放入本地同一文件夹。本项目不另建上传网页。成功退款、明确费用和成本的覆盖范围由商家确认，数据校验无法证明没有漏交交易。

在 Codex 中直接使用本地 Skill：

> 请读取并使用 F:/电商经营核对MVP/ecommerce-reconcile/SKILL.md，核对我上传的四个 Excel 文件，截止日为 YYYY-MM-DD。金额只能使用随附脚本计算，输出保存到 F:/电商经营核对MVP/运行结果/ 的新目录。

没有向全局 Skill 目录安装副本，符合“所有内容放在 F 盘”的要求。上面的完整路径可直接加载；若日后要在技能列表自动发现，可另行安装或配置目录联接。

也可以在 PowerShell 中运行：

```powershell
& 'F:/电商经营核对MVP/run.ps1' -InputDir 'F:/电商经营核对MVP/客户数据/某批次' -AsOf '2026-09-19'
```

不要直接使用尚未创建的示例客户目录；替换成四表实际所在目录。输出使用全新的目录，不会覆盖旧报表。Excel 是计算快照，调整数据必须改来源的新副本后重跑。

## 验证与维护

```powershell
& 'F:/电商经营核对MVP/run.ps1' -Test
```

50 组模拟 Excel 场景和逐组预期/实际结果在 `验证/模拟测试/`；测试摘要在 `验证/测试报告.md`。已检查两份演示报表的金额、缺失值、公式安全、原文件哈希和防覆盖行为。未接入真实商家数据，也未进行 Microsoft Excel 桌面程序自动化测试；文件已通过独立读取和渲染检查。

已验证环境：本机 Codex 捆绑 Python、openpyxl 3.1.5、Node.js、@oai/artifact-tool。代码无网络请求。生成的新文件存储于本地；在 Codex 中上传内容仍遵循所用产品的数据处理方式。移动到其他电脑时需有对应运行依赖，静态 Excel 和 PDF 可直接打开。
